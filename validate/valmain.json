{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "Check_Header",
  "type": "object",
  "title": "The Root Schema",
  "required": [
    "method",
    "settings"
  ],
  "properties": {
    "method": {
      "$id": "#/properties/method",
      "type": "string",
      "title": "The Method Schema",
      "default": "",
      "examples": [
        "getSeats"
      ],
      "pattern": "^(getLines)|(getSeats)|(getCategories)|(getSectors)$"
    },
    "settings": {
      "$id": "#/properties/settings",
      "type": "object",
      "title": "The Settings Schema",
      "required": [
        "filter",
        "fields"
      ]
  
    }
  }
}